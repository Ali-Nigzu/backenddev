[project]
name = "backenddev"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "numpy>=1.26,<3",
  "torch>=2.2,<3",
  "timm>=1.0,<2",
  "opencv-python-headless>=4.9,<5",
  "ultralytics>=8.3,<9",
]

[tool.setuptools]
packages = ["assemble", "contracts", "detect", "track", "events", "demographics", "demographics._mivolo", "demographics._mivolo.model"]
